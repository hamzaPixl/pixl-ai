id: detect-context-epic
name: Detect Epic Context
description: Build structured project context for an epic
version: 2.0.0
tier: utility
workflow_format: v2
session_mode: plugin
routing:
  auto_route: false
  sub_invocable: true
  category: general

stages:
  - id: detect-context-epic
    name: Detect Epic Context
    type: task
    max_turns: 30
    prompt: |
      Explore the codebase and build structured project context for an epic.
      Identify relevant files, patterns, architecture, and scope boundaries.
    contract:
      structured_output: true
